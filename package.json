{
  "name": "tiled_extension_cbin",
  "version": "1.0.0",
  "description": "Extension for tiled to export a custom map",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "tiled"
  ],
  "author": "Me",
  "license": "ISC",
  "devDependencies": {
    "@mapeditor/tiled-api": "^1.9.2"
  }
}
